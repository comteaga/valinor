import styled, { keyframes } from 'styled-components';

const Container = styled.div`
  font-size: 20px;
  display: flex;
  justify-content: center;
  align-items: center;
  margin: auto;
`;

export { Container };
